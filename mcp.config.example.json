{
  "//": "Example of the ONE file that connects Git/ticket tools. Copy to mcp.config.json (git-ignored) and set each *_TOKEN in .env. `tokenEnv` is a reference, never a secret. Pinned commands below match package.json + verified local runs; Bitbucket stdio is the local default, SSE URL is the hosted alternative.",
  "servers": {
    "github": {
      "transport": "stdio",
      "command": "npx",
      "args": ["-y", "github-mcp-server-js"],
      "tokenEnv": "GITHUB_TOKEN"
    },
    "gitlab": {
      "transport": "stdio",
      "command": "npx",
      "args": ["-y", "@zereight/mcp-gitlab"],
      "tokenEnv": "GITLAB_TOKEN"
    },
    "bitbucket": {
      "transport": "stdio",
      "command": "npx",
      "args": ["-y", "bitbucket-mcp"],
      "tokenEnv": "BITBUCKET_TOKEN"
    },
    "jira": {
      "transport": "sse",
      "url": "https://mcp.atlassian.com/sse",
      "tokenEnv": "JIRA_TOKEN"
    }
  }
}
