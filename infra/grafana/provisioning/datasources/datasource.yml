# Grafana datasource provisioning (day-04 §3.4).
apiVersion: 1
datasources:
  - name: Prometheus
    uid: harness-prom
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true